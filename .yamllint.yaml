# Gencraft Studio - Canonical yamllint Configuration
# SSoT: gcs-devops-standards/.yamllint.yaml
extends: default

rules:
  # Disabling line-length since config database files contain long lines
  line-length: disable

  # Disabling document-start since --- is not standard for local config files
  document-start: disable

  # Standardise l'indentation des commentaires.
  comments-indentation: enable

  # Autorise les commentaires en ligne avec un seul espace.
  comments:
    require-starting-space: true
    min-spaces-from-content: 1
